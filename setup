#!/usr/bin/env sh
# Run Baseline setup with deprecation warnings silenced. Invokes the script
# directly so Composer is only used inside the script (where output is filtered).
set -e
cd "$(dirname "$0")"
exec php -d error_reporting=22527 scripts/baseline-setup.php "$@"
